# Example env file for backend secrets. Copy this to `backend/.env` and set the real values.
# IMPORTANT: Do NOT commit the real `.env` file to source control.

# GROQ API key used by chat/ML integrations. Example value omitted.
GROQ_API_KEY=
